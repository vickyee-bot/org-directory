export default function Header() {
  return (
    <div className="h-16 bg-white flex items-center px-4 border-b">
      <div className="font-semibold text-lg">Dashboard</div>
      {/* You can add a search bar or profile menu on the right later */}
    </div>
  );
}
